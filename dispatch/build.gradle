plugins {
    id 'java'
    id 'java-library'
    id 'maven-publish'
}

group 'com.mrwind.dispatch'
version '0.6.0'

sourceCompatibility = 1.8

dependencies {
}

task sourcesJar(type: Jar) {
    from sourceSets.main.allJava
    classifier = 'sources'
}

publishing {
    publications {
        mavenJava(MavenPublication) {
            artifactId = 'dispatch-algorithm'
            from components.java
            artifact sourcesJar
            pom {
                name = 'dispatch-algorithm'
                description = 'dispatch-algorithm'
            }
        }
    }
    repositories {
        maven {
            url = 'http://nexus.tooc.in/repository/maven-releases'
            credentials {
                username = 'admin'
                password = 'admin123'
            }
        }
    }
}